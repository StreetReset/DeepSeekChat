[project]
name = "test-mexica"
version = "0.1.0"
description = "Console chat assistant that uses Ollama and DeepSeek-R1"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyinstaller>=6.0",
]
